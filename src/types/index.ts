export interface Contact {
    phone: string;
}

export interface Customer {
    id: number;
    name: string;
    email: string;
    phone: string;
    address: string;
    registration_date: string;
    createdAt: string;
}